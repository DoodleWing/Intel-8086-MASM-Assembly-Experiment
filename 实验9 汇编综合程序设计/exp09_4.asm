	DATA SEGMENT
	BUF DB 10,39,18,21,79,65,33,43,22,62
	COUNT DW $-ARRAY
	DATA ENDS

	STACK SEGMENT STACK

	STACK ENDS

	CODE SEGMENT
		ASSUME DS:DATA,SS:STACK,CS:CODE
	START:
		MOV AX,DATA
		MOV DS,AX
		
		MOV AX,COUNT
		SUB AX,1
		MOV CX,AX
		MOV SI,1	
		
	i:	
		MOV	AL,[BUF+SI]
		MOV DX,CX
		MOV CX,SI
	k:	
		MOV DI,CX
		MOV AH,[BUF+DI-1]
		SUB AH,AL
		JS	bre
		MOV	BL,[BUF+DI-1]
		MOV [BUF+DI],BL
		JMP _in
	bre:
		JMP	_ou
	_in:	
		LOOP k
	_ou:	
		MOV BL,AL				
		MOV [BUF+DI],BL
		MOV CX,DX
		INC SI
		LOOP i
		
		MOV AH,4CH
		INT 21H
		
	CODE ENDS
		END START